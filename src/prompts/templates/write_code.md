# Write Code

You are writing code for a project. Follow the project conventions exactly.

## Task

Write code for the following specification:

Spec: {{spec}}
Target file: {{targetFile}}
Language: {{language}}

## Project Conventions

{{conventions}}

## Guidelines

- Write only the code — no explanations, no markdown fences, no preamble.
- Follow existing naming conventions, indentation style, and import patterns from the project.
- Handle error cases explicitly — never swallow exceptions silently.
- Prefer clarity over cleverness. If the next developer has to think twice, simplify.
- Do not introduce abstractions not demanded by the spec.
- Output only the complete file content, ready to write to disk.
